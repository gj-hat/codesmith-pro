## 替换模板
## package com.gjstudy.velocitystudy.web.controller;
package ${package}.controller;

import ${package}.web.domain.${className};
import ${package}.web.service.${className}Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.List;

@RestController
@RequestMapping("/${genUtils.bigToSmall(${className})}")
public class ${className}Controller {

    @Autowired
    private ${className}Service ${genUtils.bigToSmall(${className})}Service;


    @RequestMapping("/list")
    public List<${className}> list() {
        return ${genUtils.bigToSmall(${className})}.list();
    }

    @RequestMapping("/save")
    public void save(@RequestBody ${className} ${genUtils.bigToSmall(${className})}) {
            ${genUtils.bigToSmall(${className})}Service.save();
    }

    @RequestMapping("/update")
    public void update(@RequestBody ${className} ${genUtils.bigToSmall(${className})}) {
            ${genUtils.bigToSmall(${className})}Service.update(${genUtils.bigToSmall(${className})});
    }

    @RequestMapping("/del")
    public void del(@RequestBody int[] ids) {
            ${genUtils.bigToSmall(${className})}Service.del(ids);
    }


}
